apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: gitops-repo
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://github.com/nusissSEteam9/git-ops
  ref:
    branch: main
  secretRef:
    name: flux-git-auth  
